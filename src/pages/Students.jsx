import { useEffect, useState } from 'react'
import { listStudents } from '../services/studentService'
export default function Students(){const [rows,setRows]=useState([]); const [q,setQ]=useState(''); useEffect(()=>{listStudents().then(setRows).catch(alert)},[]); const filtered=rows.filter(r=>JSON.stringify(r).toLowerCase().includes(q.toLowerCase())); return <><div className="titlebar"><div><h1>Học viên cao học</h1><p>Quản lý hồ sơ học viên cao học</p></div><button className="primary">+ Thêm học viên</button></div><input className="search" placeholder="Tìm theo mã, họ tên, ngành..." value={q} onChange={e=>setQ(e.target.value)}/><section><table><thead><tr><th>Mã HV</th><th>Họ tên</th><th>Chương trình</th><th>Khóa</th><th>Trạng thái</th><th>Thao tác</th></tr></thead><tbody>{filtered.map(r=><tr key={r.id}><td>{r.code}</td><td>{r.full_name}</td><td>{r.program}</td><td>{r.cohort}</td><td><span className="badge">{r.status}</span></td><td>👁 ✏️ 🗑️</td></tr>)}</tbody></table></section></>}
